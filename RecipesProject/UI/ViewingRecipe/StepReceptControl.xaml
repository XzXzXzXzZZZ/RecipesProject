<UserControl x:Class="RecipesProject.UI.ViewingRecipe.StepReceptControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="#FFF9F9F9"
            BorderBrush="#FFCCCCCC"
            BorderThickness="1"
            CornerRadius="5"
            Padding="10"
            Margin="0,0,0,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label x:Name="NumberStep"  
                   Grid.Row="0"
                   Content="Шаг: "
                   FontSize="16"
                   FontWeight="Bold"
                   Foreground="#FF333333"
                   Padding="0"
                   Margin="0,0,0,8"/>

            <Image x:Name="ViewImage" Grid.Row="1"/>

            <TextBlock x:Name="DescriptionStep" 
                       Grid.Row="2"
                       Text="Описание"
                       TextWrapping="Wrap"
                       FontSize="16"
                       Foreground="#FF444444"/>
        </Grid>
    </Border>
</UserControl>